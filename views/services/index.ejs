<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <title>Service Providers</title>
    <style>
        .profile-card {
            border: none;
            margin-bottom: 2rem;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .card-img-top {
            height: 200px;
            object-fit: cover;
            border-radius: 0.5rem;
        }

        .nav-link {
            color: #007bff;
            text-decoration: none;
        }

        .nav-link:hover {
            text-decoration: underline;
        }

        nav {
            padding: 1rem 0;
        }

        h4 {
            font-size: 1.2rem;
        }

        .card-body {
            padding: 1rem;
        }

        .btn-primary {
            background-color: #007bff;
            border: none;
        }

        @media (max-width: 768px) {
            nav {
                flex-direction: column;
                align-items: center;
            }

            nav a {
                margin-bottom: 0.5rem;
            }
        }
    </style>
</head>

<body>
    <nav class="d-flex justify-content-around">
        <% if(!currentUser){ %>
        <a class="nav-link" href="/services/signup">
            <h4>SignUp As Service Provider</h4>
        </a>
        <a class="nav-link" href="/services/login">
            <h4>Login As Service Provider</h4>
        </a>
        <a class="nav-link" href="/user/signup">
            <h4>SignUp (User)</h4>
        </a>
        <a class="nav-link" href="/user/login">
            <h4>Login (User)</h4>
        </a>
        <% }else{ %>
        <a class="nav-link" href="/logout">
            <h4>Log Out</h4>
        </a>
        <% } %>
    </nav>

    <% if(success){ %>
    <div class="alert alert-success" role="alert">
        <h1><%= success %></h1>
    </div>
    <% } %>

    <div class="container">
        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
            <% for(let service of services){ %>
            <div class="col">
                <div class="profile-card card h-100">
                    <img src="https://images.unsplash.com/photo-1552733407-5d5c46c3bb3b?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxzZWFyY2h8MTB8fHRyYXZlbHxlbnwwfHwwfHx8MA%3D%3D&auto=format&fit=crop&w=800&q=60"
                        class="card-img-top" alt="Profile">
                    <div class="card-body">
                        <h5 class="card-title"><%= service.serviceCategory %></h5>
                        <p class="card-text">
                            <%= service.username %> <br>
                            <%= service.serviceArea.city %>, <%= service.serviceArea.state %>
                        </p>
                        <a href="/services/<%= service._id %>" class="btn btn-primary">See Profile</a>
                    </div>
                </div>
            </div>
            <% } %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
